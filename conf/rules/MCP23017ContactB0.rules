rule "Contact state of LED0 Changed"
when
    Item MCP23017ContactB0 changed

then
    if(MCP23017ContactB0.state == OPEN) {
        MCP23017ContactB0_LED.sendCommand(ON)
    } else {
        MCP23017ContactB0_LED.sendCommand(OFF)
    }
end