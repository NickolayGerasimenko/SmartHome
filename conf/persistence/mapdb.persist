Strategies {
        default = everyUpdate
}

Items {
        // persist all items on every change and restore them from the db at startup
        v_gLights* : strategy = everyChange, restoreOnStartup
        gWallButtons* : strategy = everyChange
}